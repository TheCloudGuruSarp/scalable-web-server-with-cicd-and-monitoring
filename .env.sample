# Backend environment variables
PORT=8080

# Frontend environment variables
# (Note: These are typically build-time args for React, but for this simple setup, they are just for documentation)
REACT_APP_API_URL=http://localhost:8080
